import { NavbarAutocloseDirective } from './navbar-autoclose.directive';

describe('NavbarAutocloseDirective', () => {
  it('should create an instance', () => {
    const directive = new NavbarAutocloseDirective();
    expect(directive).toBeTruthy();
  });
});
